# CJAS Recovery Kit v1 规范

状态：P1 设计基线；实现和参数仍需测试验证。本文不得被描述为已完成能力。

## 1. 产品定义

Recovery Kit 是用户持有的独立恢复能力载体，文件扩展名为 `.cjas`。用户只需理解“Kit＋恢复密码”，不需要理解内部密钥、哈希、存储位置或交易标识。

Kit 必须：

- 由用户主动下载和保存，不自动上传。
- 经过恢复密码保护。
- 不包含 Vault 明文或附件明文。
- 不把完整解密条件交给 CJAS 平台。
- 可配合独立恢复工具在新设备、新浏览器和 CJAS 平台不可用时恢复。
- 对应一个确定的、完整且独立可恢复的 Vault 版本。
- 不因用户停止续费而失效。
- 不因用户忘记账户、CJAS 网站关闭或公司停止运营而失效。
- 每个 Kit 只对应一个完整 Vault 版本，不得与其他版本共享恢复能力。

## 2. 推荐密码学分层

每个 Vault 版本生成：

1. 随机 32 B 数据加密密钥 `DEK`。
2. 随机 12 B Vault nonce。
3. 使用 AES-256-GCM 以 DEK 加密完整 Vault 快照。
4. 用户恢复密码经 KDF 和随机 salt 派生 32 B 密钥加密密钥 `KEK`。
5. 使用 AES-256-GCM 以 KEK 加密 DEK，得到 `wrapped_dek`。
6. `.cjas` Kit 保存 KDF 参数、`wrapped_dek`、密文定位信息和密文 SHA-256，不保存明文密码或明文 DEK。

平台只接收 Vault 密文和最少存储元数据。平台没有恢复密码和完整解密条件。

## 3. `.cjas` 内部格式建议

### 3.1 容器

推荐采用二进制容器而不是用户可读 JSON：

- 固定魔数：`CJASKIT`。
- 容器版本：`1`。
- 头部长度和载荷长度采用明确字节序。
- 载荷使用规范化 CBOR 或确定性编码；实现前必须用跨语言测试向量固定编码结果。
- Kit 整体不得包含 Vault 明文。

如果 4—6 周内确定性 CBOR 工具链风险过高，可暂用 UTF-8 JSON 作为内部编码，但必须封装在 `.cjas` 二进制外壳中、严格 schema 校验，且 UI 不向用户展示内部内容。

### 3.2 建议字段

| 字段 | 说明 | 敏感性 |
|---|---|---|
| `kit_version` | Recovery Kit 格式版本 | 公开 |
| `vault_format` | Vault 密文格式版本 | 公开 |
| `snapshot_id` | 随机版本标识，不含用户身份 | 公开 |
| `kdf` | 算法、salt 和参数 | 公开 |
| `wrap_algorithm` | DEK 包装算法及 nonce | 公开 |
| `wrapped_dek` | 密码保护后的 DEK | 高敏感但不可单独解密 |
| `ciphertext_sha256` | 对应 Vault 密文哈希 | 公开 |
| `storage_locators` | 一个或多个公开密文定位符 | 公开但可能关联活动 |
| `created_at` | 版本创建时间 | 可选公开元数据 |
| `tool_compatibility` | 最低恢复工具版本 | 公开 |

默认不放入：用户名、邮箱、资产类别、原始文件名、联系人、原始内容哈希、余额或任何 Vault 内容摘要。

### 3.3 完整性和认证

- `wrapped_dek` 的 AES-GCM Additional Authenticated Data 应覆盖所有安全关键字段的规范编码，包括版本、KDF 参数、snapshot ID、密文哈希和存储定位符。
- 修改任何受保护字段都必须导致解包失败。
- 密文本身另有 SHA-256 和 AES-GCM 认证。
- 未知关键字段、重复字段、非法长度和非规范编码必须拒绝。

## 4. KDF 候选与 Compatibility Study

### 4.1 候选 A：Argon2id

优点：内存困难，能提高离线密码猜测成本；适合保护可被复制的 Kit。
缺点：浏览器需要经过审计并随独立工具打包的 WASM/实现；长期兼容、启动时间和低内存设备需要测试。

建议起始参数仅作为测试基线：64 MiB 内存、3 次迭代、并行度 1、16 B 随机 salt、32 B 输出。正式参数应在目标设备上校准，目标派生时间约 500—1500 ms，并设置明确的最低安全参数。

### 4.2 候选 B：PBKDF2-HMAC-SHA-256

优点：Web Crypto 原生支持、跨平台成熟、独立恢复工具更容易长期实现。
缺点：不是内存困难算法，对 GPU 离线猜测的抵抗力弱于 Argon2id，需要更强用户密码和足够迭代次数。

迭代参数不得硬编码为未经测试的永久值；应记录在每个 Kit 内并允许未来版本提高。

### 4.3 当前状态

**当前不锁定 Argon2id 或 PBKDF2。** 必须先完成 Compatibility Study，再由 CEO 决策 Kit v1 默认 KDF。研究范围至少覆盖 Chrome、Safari、Edge、Windows 和 macOS，并比较安全性、执行时间、峰值内存、依赖打包、离线恢复、长期兼容和跨实现测试向量。

无论最终选择何种算法：

- 核心实现必须可独立审计并保留未来开源能力。
- 关键依赖必须本地打包，不得从 CDN 动态加载。
- Kit 必须记录 KDF 算法和参数。
- 恢复工具禁止静默降级或替换算法。

## 5. 密钥与密码生命周期

### 5.1 创建

- DEK、nonce 和 salt 由操作系统支持的浏览器密码学随机源生成。
- 密码只存在于当前内存和 KDF 调用范围。
- DEK 只存在于加密和包装所需内存。
- 不写入日志、DOM、URL、错误报告、遥测或浏览器持久存储。
- 完成、取消、超时或出错后移除引用；JavaScript 无法保证物理内存立即清零，此限制必须进入威胁模型。

### 5.2 恢复

- 用户选择 Kit 后才请求密码。
- 先进行容器和参数上限检查，再运行 KDF，防止恶意 Kit 触发资源耗尽。
- KEK 仅用于解包 DEK；DEK 仅用于解密指定快照。
- 恢复结束或失败后清理状态，不缓存密码。

### 5.3 平台

- 平台不得接收密码、KEK、DEK 或未加密 Kit。
- 本期不保存任何恢复片段。
- 员工和代付服务不得拥有单独解密能力。

## 6. 生成、保存和更新

### 6.1 生成

1. 结构化 Vault 形成完整快照。
2. 生成新 DEK 并加密快照。
3. 用恢复密码派生 KEK，包装 DEK。
4. 密文保存成功并取得内部定位信息。
5. 生成 `.cjas` Kit。
6. 用户主动下载并确认找到 Kit。
7. 隔离会话演练通过后版本完成。

### 6.2 保存指导

- 至少两个由用户控制的独立位置。
- Kit 与恢复密码不要保存在同一未保护位置。
- 文件名可读但不包含资产详情或密码。
- 建议同时保存独立恢复工具的签名发行包和校验说明。

### 6.3 更新

- 每次更新生成新的 snapshot ID、DEK、Vault nonce、KDF salt、密文和 Kit。
- 推荐用户可以沿用同一恢复密码，但密码必须重新输入，系统不得保存旧密码。
- 旧 Kit 继续恢复旧版本，不自动重写或吊销。
- 新 Kit 不依赖旧 Kit。
- 任意两个版本不得共享 DEK、wrapped DEK、salt、nonce 或 Kit；一个版本的 Kit 不得恢复另一版本。

## 7. 恢复流程

1. 验证 Kit 魔数、长度、编码和版本。
2. 验证 KDF 参数在安全且可执行的允许范围内。
3. 输入恢复密码并派生 KEK。
4. 解包 DEK；失败统一显示密码错误、Kit 不匹配或 Kit 损坏，不泄露内部细节。
5. 从 Kit 内定位符读取密文，或由用户导入本地密文副本。
6. 校验密文 SHA-256；不一致立即停止。
7. 用 DEK 进行 AES-GCM 解密。
8. 严格解析 Vault 快照并核对各条目及附件哈希。
9. 只在全部通过后显示和导出恢复内容。

## 8. 平台外恢复

独立恢复工具必须：

- 作为版本固定的静态离线包交付，核心依赖本地打包，不依赖 CJAS API 或 CDN。
- 能读取 `.cjas` Kit、提示恢复密码、直接访问公开密文存储。
- 支持多个只读端点和导入本地密文副本。
- 不含钱包、上传、签名、遥测或账户登录代码。
- 发布包提供版本号、SHA-256 和可验证构建记录。
- 至少支持 CEO 验收范围内的主流桌面浏览器；具体长期支持范围需另行决定。

## 9. 错误与失败模型

| 失败 | 必须行为 |
|---|---|
| 错误密码 | 解包失败，不下载/不显示明文 |
| 错误 Kit | schema 或认证失败，fail-closed |
| Kit 篡改 | AAD/GCM 或规范编码校验失败 |
| 不支持版本 | 明确要求兼容工具，不猜测解析 |
| 恶意 KDF 参数 | 在 KDF 前拒绝，防止资源耗尽 |
| 密文不存在 | 明确不可取得，允许重试或本地导入 |
| 密文哈希不符 | 不进入解密 |
| 密文认证失败 | 不输出任何部分内容 |
| 快照条目/附件哈希不符 | 整体恢复 FAIL，不标记成功 |
| 内存不足 | 清理状态，不生成半成品 |
| 用户忘记密码 | 明确不可恢复；平台无绕过机制 |

## 10. 向后兼容与迁移

- Kit 使用独立 `kit_version`，Vault 密文使用独立 `vault_format`。
- 恢复工具必须按版本显式分派，未知版本拒绝。
- 已完成 Kit 永不被后台自动修改。
- 新工具至少读取所有已正式发布的旧 Kit 格式；停止支持前必须提供可离线保存的旧工具。
- 格式迁移通过“读取旧版本→本机恢复→生成全新完整版本和新 Kit”完成，不就地篡改旧 Kit。
- v1.0 主网密文格式保留只读解析能力；没有对应密钥时不得声称可独立恢复。
- v1.1 明文 JSON 凭证只作为测试参考，不成为正式 Kit 格式。

## 11. 测试要求

- 固定跨实现测试向量：密码、salt、KDF 输出、wrapped DEK、密文和恢复结果。
- 正确密码、错误密码、Unicode 密码、边界长度和规范化策略。
- 每个字段篡改、截断、重复、未知版本和恶意长度。
- KDF 参数资源上限。
- 新设备、新浏览器、离线包和平台不可用恢复。
- 两个完整版本和两个 Kit 互不混用。
- 日志、DOM、URL、存储和错误报告无密码或密钥。

## 12. CEO 决策状态

- **待决策**：Compatibility Study 完成后，由 CEO 选择 Kit v1 默认 KDF。工程团队必须提交明确推荐，不得以兼容性名义自行锁定或静默回退。
