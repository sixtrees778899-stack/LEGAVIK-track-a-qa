# CJAS Vault MVP 威胁模型

状态：P1 威胁模型基线；覆盖 CEO 体验版，不代表完成外部安全审计。

## 1. 安全目标

1. Vault 明文、恢复密码和完整解密条件不离开用户设备。
2. CJAS 平台、员工、代付服务和存储层均不能单独解密。
3. 错误密码、错误 Kit、篡改和版本不匹配全部 fail-closed。
4. 每个已完成版本在平台不可用和停止续费后仍可独立恢复。
5. 用户恢复的是创建时的完整正确快照，而不是未验证或部分内容。
6. 一版本一 Kit；任何版本之间不得共享恢复能力。

## 2. 需要保护的资产

- Vault 结构化明文和附件。
- 用户恢复密码。
- DEK、KEK 和任何中间密钥材料。
- Recovery Kit。
- 用户身份、版本关系和存储定位等关联元数据。
- 代付钱包和平台基础设施凭证。
- 独立恢复工具的代码完整性和发布签名。
- 测试证据、审计日志和版本状态的真实性。

## 3. 攻击者

- 获取 Kit 或密文副本的外部攻击者。
- 猜测恢复密码的离线攻击者。
- 钓鱼网站或伪造恢复工具。
- 用户设备上的恶意软件、浏览器扩展或剪贴板监听器。
- 被入侵的 CJAS 前端、CDN、构建或依赖供应链。
- 恶意或被入侵的 CJAS 员工账户。
- 被入侵的代付服务或存储服务。
- 恶意构造 Kit、密文、文件名或附件的输入者。
- 试图关联用户身份和链上活动的观察者。

## 4. 信任边界与权限

### 4.1 用户设备

允许：处理 Vault 明文、密码、KDF、DEK、加解密和本地导出。
禁止：把敏感材料写入日志、URL、浏览器持久存储或遥测。
假设：正常使用时设备和浏览器未被完全控制；完全失陷设备属于残余风险。

### 4.2 CJAS 前端

允许：提供结构化 UX、在本地执行加密、把密文提交给存储接口。
禁止：上传明文、密码、Kit、DEK 或恢复内容；不得动态引入未经固定的关键密码学代码。

核心加密模块使用公开标准算法，必须与 UI、账户和存储代码隔离，具备独立审计边界并保留未来开源能力。CJAS 不自行设计密码算法。

### 4.3 代付服务

允许：接收已经加密的字节、执行费用检查、存储提交并返回不透明定位结果。
禁止：接收 Vault 明文、密码、Kit 或 DEK；不得为任意来源无限代付。

### 4.4 存储层

允许：长期保存和读取密文及最少必要元数据。
禁止：获得解密条件；不得被视为唯一恢复入口。

### 4.5 独立恢复工具

允许：本机读取 Kit 和密码、直接读取公开密文、解密并导出。
禁止：钱包、上传、签名、账户登录、遥测和向 CJAS 回传恢复信息。

### 4.6 员工和运维

允许：管理密文存储状态、代付额度和非敏感运行指标。
禁止：查看 Vault 内容、密码、Kit、DEK；任何单个员工不得恢复用户资料。

### 4.7 轻量账户系统

允许：登录、订单、支付、Vault 名称、创建/更新时间、Version 状态、演练状态、提醒、客服及必要公开定位信息。

禁止：Vault 明文、Recovery Password、完整 Data Key、完整 Recovery Kit 或任何可单独恢复 Vault 的信息。账户系统不可成为独立恢复的数据或权限依赖。

## 5. 数据流

### 创建

用户输入/附件 → 本机结构化快照 → 随机 DEK 加密 → Vault 密文 → 代付服务 → 存储层。
恢复密码 → 本机 KDF → KEK → 包装 DEK → `.cjas` Kit → 用户主动保存。

### 恢复

Kit＋密码 → 本机恢复工具 → 解包 DEK。
Kit 内公开定位符 → 存储层读取密文 → 哈希校验 → 本机解密 → 完整性校验 → 用户导出。

敏感数据不得跨越用户设备边界。

## 6. 主要威胁与控制

| 威胁 | 控制 |
|---|---|
| Kit 泄露 | 强密码、Argon2id、随机 salt、DEK 包装、保存指导 |
| 密码泄露 | Kit 分开保存；密码不记录、不上传；演练后清理引用 |
| Kit＋密码同时泄露 | 无密码学补救；明确残余风险和用户指导 |
| 离线暴力猜测 | 内存困难 KDF、最低密码要求、参数可升级 |
| 钓鱼恢复工具 | 离线发行包、版本哈希、签名/校验说明、反钓鱼文案 |
| 恶意浏览器扩展/软件 | 最小 DOM 暴露、无剪贴板密钥、建议在可信设备恢复；完全失陷为残余风险 |
| 前端供应链注入 | 固定依赖、禁止关键代码 CDN 动态加载、CSP、可复现构建、发布校验 |
| 内部员工滥用 | 平台无解密条件、最小权限、操作审计、代付与内容隔离 |
| 代付服务滥用 | 配额、大小上限、请求认证、费用上限、幂等和人工熔断 |
| 存储篡改 | 密文 SHA-256、AES-GCM 认证、多端点读取 |
| 元数据关联 | 链上最少元数据、随机 snapshot ID、不上传用户内容摘要 |
| 恶意 Kit 参数 DoS | KDF 和长度参数上限，在分配和派生前校验 |
| 恶意文件名/内容 | 不使用 `innerHTML`、输出名净化、严格边界解析 |
| 版本降级 | AAD 绑定版本、未知版本拒绝、禁止静默降级 |
| 部分恢复误判 | 所有条目和附件校验完成前不显示 PASS |
| 平台消失 | Kit、公开存储、独立工具和本地密文导入 |
| 停止续费 | 解密不依赖账户或订阅 API |
| 账户遗失 | Kit＋Password 恢复不要求登录、账户找回或客服批准 |
| 版本间密钥复用 | 每版新 DEK、salt、nonce、wrapped DEK 和 Kit；交叉恢复必须失败 |

## 7. 内部员工场景

- 员工可看到的最多是账户运营信息、密文字节、大小、时间和存储状态。
- 默认不得把公开存储定位符与用户身份暴露给不需要的岗位。
- 代付钱包私钥必须由服务端受限组件管理，与 Vault 密钥系统完全分离。
- 生产期需要双人审批、关键操作审计和权限定期复核；CEO 体验版至少实现密钥分离和最小权限配置。

## 8. 平台消失场景

恢复不得调用 CJAS 登录、订阅、索引或密钥服务。Kit 必须携带独立恢复所需的公开定位信息和密码保护的 DEK。独立工具应支持备用读取端点和本地密文导入。

如果公开存储本身永久不可用且用户没有密文副本，恢复可能失败；这是需明确披露的残余风险。

## 9. 可接受残余风险

- 用户设备在输入密码或查看明文时已被完全控制。
- 用户主动把 Kit 和密码交给同一攻击者。
- 用户忘记密码且没有备用安排。
- 用户丢失所有 Kit 副本。
- 所有长期存储端点和用户本地密文副本同时不可用。
- 极弱密码在长期离线攻击下被猜中。
- JavaScript 运行时无法保证密钥物理内存立即清零。

这些风险必须通过说明、密码要求、保存指导和演练降低，但平台不得虚假承诺消除。

## 10. 不可接受风险

- 明文、密码、DEK 或未保护恢复秘密离开用户设备。
- 密钥、密码或 Kit 内容进入日志、Git、URL、遥测或浏览器持久存储。
- 平台或单个员工能够独立解密。
- 错误或篡改输入仍生成恢复文件。
- 订阅或平台在线状态成为已有版本恢复条件。
- 普通用户必须操作钱包或底层存储标识。
- 使用未固定的远程密码学代码执行加解密。
- 未通过独立恢复演练却把版本标记为完成。
- 账户、支付、客服或版本索引包含任何可单独恢复 Vault 的材料。
- 两个 Vault 版本共享 Data Key 或 Recovery Kit 恢复能力。

## 11. CEO 体验版前安全门槛

全部满足才可进入 CEO 体验：

1. 完成数据流和信任边界代码审查。
2. 固定关键依赖并生成依赖清单。
3. 密码、密钥、Kit 和明文的静态及运行时泄露扫描为零。
4. 正确恢复及所有主要负向测试自动化 PASS。
5. 恶意长度、KDF 参数、文件名和容器 fuzz/边界测试无伪恢复。
6. 独立恢复工具在无 CJAS API 情况下 PASS。
7. 两个完整版本可分别恢复，互换 Kit 必须 FAIL。
8. 代付服务在模拟环境验证费用上限、大小限制和幂等。
9. 恢复工具不含钱包、签名、上传和遥测调用。
10. 业务基线文件哈希和 Git 变更范围经过审计。

## 12. 进入主网前额外门槛

- CEO 对完整本地流程验收通过。
- 针对代付和永久存储进行专项风险复核。
- 明确单次费用上限和一次性授权。
- 使用无真实资产的钱包及非敏感测试数据。
- 防止重复广播并保留可审计证据。
- 主网测试不是 CEO 体验版开发的默认步骤。

## 13. CEO 决策状态

- 已确认：内置客户端本地加密、轻量账户＋完全独立恢复、一版本一 Kit。
- 待决策：Compatibility Study 后的默认 KDF。
- 工程推荐继续以 Chrome、Edge、Safari 桌面端作为 CEO 体验范围；Windows 与 macOS 均纳入 KDF 和恢复兼容测试。
