# 安全说明

## 已确认措施

- 文件内容使用 AES-GCM 加密。
- 每次加密使用浏览器安全随机源生成密钥和 nonce。
- 原文件和密文均计算 SHA-256，用于完整性核验。
- 钱包签名通过 Wander 注入 API 完成；基线代码未包含私钥。
- 密钥不会随密文上传，验收日志也明确标注“不上传不保存”。

## 敏感信息基线扫描

版本固化前扫描未发现：

- `.env` 或 `.env.*`
- 私钥、PEM/JWK/keystore 文件
- 助记词或 seed phrase
- 钱包导出文件
- API secret、wallet key 或 private key 赋值

目录中包含公开链上交易 ID 和钱包地址。它们是验收证据，不是私钥，但会关联公开链上活动。

## 已知风险

1. v1.0 加密密钥仅存于当前页面内存；关闭页面后，链上密文无法独立恢复。
2. 恢复流程依赖同一会话中的密钥状态，不能视为完整的长期传承方案。
3. Arweave 网关数据索引存在延迟，单次读取失败不能直接判定数据丢失。
4. Arweave SDK 从 CDN 动态加载，当前没有本地固定依赖或供应链完整性锁定。
5. 单页代码缺少自动化安全测试、内容安全策略和正式威胁模型。
6. 主网交易不可撤销且会产生费用，任何复验必须防止重复签名和重复广播。

## 操作规则

- 不在代码、文档、日志、截图或 Git 中保存真实私钥、助记词或恢复密钥。
- 不把恢复凭证与链上密文存放在同一公开位置。
- 默认使用本地测试和模拟数据；主网签名与广播必须单独获批。
- 如发现疑似敏感文件，只报告路径和风险，不显示其内容。

