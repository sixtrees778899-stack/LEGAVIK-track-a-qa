import {sanitizeFilename} from './pilot-core.js';
export function createFileDelivery({bytes,filename,mimeType}){if(!(bytes instanceof Uint8Array)||!bytes.length)throw new Error('恢复文件字节无效。');const safe=sanitizeFilename(filename);return{filename:safe,mimeType,size:bytes.length,download({documentRef=document,urlApi=URL,schedule=setTimeout}={}){let url;try{url=urlApi.createObjectURL(new Blob([bytes],{type:mimeType}));const link=documentRef.createElement('a');link.href=url;link.download=safe;documentRef.body.appendChild(link);link.click();link.remove();schedule(()=>urlApi.revokeObjectURL(url),1000);}catch{if(url)urlApi.revokeObjectURL(url);throw new Error('下载未能启动，请允许浏览器下载后重试。');}}};}
