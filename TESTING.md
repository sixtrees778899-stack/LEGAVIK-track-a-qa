# 测试与验收

## v1.0 已有证据

- 正式 URL：`http://localhost:8080/`
- 主网交易广播：HTTP 202。
- 交易确认：区块 1968820，报告观测确认数不少于 5。
- 原始 PDF：663 B。
- 上传及下载密文：767 B，SHA-256 均为 `dbaf32fef17653096d79c7aeaa2d9e156cfbb6af5ad0fcd29f1e5ce81dc6e1ca`。
- 原始及恢复 PDF：SHA-256 均为 `e8a8d6b25b35fea15befd1556c7988465ea1b1781949ea8ae6142d1d22effdde`。
- 原始和恢复文件已记录为字节级一致。
- 恢复 PDF 已通过文件结构检查；最终视觉打开确认由 CEO 完成。

## 已知测试现象

交易确认后首次从网关读取曾返回错误尺寸数据，约十五分钟后重试得到正确密文并完成解密。这是当前必须保留的回归场景。

## 无费用最小复验

在不修改业务代码、不连接钱包、不签名、不广播的前提下：

1. 重新计算所有基线文件 SHA-256，并与 `MANIFEST.md` 和本文件记录比对。
2. 使用 `cmp` 或等效只读工具确认 `test.pdf` 与 `test_recovered.pdf` 字节一致。
3. 通过本地静态服务器加载页面，只验证页面可加载及 `file://` 防护，不点击连接、签名或上传按钮。
4. 对现有交易 ID 进行只读状态查询或网关下载时，必须明确禁用钱包交互；该步骤需要网络权限，但不产生链上费用。

## 后续候选测试

- 从 v1.1 引入独立恢复凭证前，先复核其正向、错误密钥、篡改密文和篡改凭证测试。
- 参考早期 v2 的 ArLocal 和 E2E 测试结构，但不得直接覆盖 v1.0。
- 为网关延迟增加可控重试测试，并区分 PENDING、PASS 和 FAIL。

