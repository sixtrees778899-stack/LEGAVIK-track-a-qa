# CJAS Vault MVP UX Flow

## 1. UX 原则

- 始终使用 Vault、恢复路径、Recovery Kit、恢复密码和版本等产品语言。
- 不向普通用户暴露 Wallet、AR、Transaction ID、Gateway、JSON、Key 或 Hash。
- 每一步解释用户结果，而不是底层技术动作。
- 密码不可找回必须在设置、下载 Kit、演练和完成页重复说明。
- 未通过恢复演练的版本只能是草稿或待验证，不能显示为完成。
- 所有失败必须明确、可操作且不得导出未经验证的内容。
- 每个功能和提示都应提高 Recovery Confidence；不得用更多页面或字段替代恢复质量。

## 2. 首次创建

### 2.1 欢迎与边界

- CJAS 帮助建立和验证恢复路径，不托管资产。
- 用户资料在本设备加密后才离开设备。
- 平台无法找回恢复密码。
- 本流程最后需要完成一次独立恢复演练。

用户确认后进入创建；未确认不得继续。

### 2.2 轻量账户入口

- 创建和管理新版本时，用户使用轻量账户登录。
- 账户页面只展示订单、支付、Vault 名称、时间、Version 状态、演练状态、提醒和客服入口。
- 页面明确说明账户不保存 Vault 内容、Recovery Password、完整 Data Key 或 Recovery Kit。
- 恢复首页始终提供“不登录，使用 Recovery Kit 恢复”的独立入口。
- 忘记账户不得引导用户误以为恢复能力丢失。

### 2.3 Vault 概览

输入 Vault 名称、版本说明、核对日期和可选提醒。提示不要把完整助记词直接当作 Vault 名称或摘要。

## 3. Recovery Map 构建

### 3.1 Recovery Map 总览

总览围绕 Asset、Location、Contact、Device、Order、Hint、Warning 和 Attachment 展示关系。用户首先看到“恢复路径是否完整”，而不是资产数量。

### 3.2 Asset

用户选择分类并创建条目。每类提供普通语言示例，但不要求余额、私钥或助记词。

### 3.3 Location、Device 与 Contact

用户为 Asset 关联资料位置、所需设备和联系人。界面说明这些实体只是恢复知识，不等同于授权或自动访问能力。

### 3.4 Order、Hint 与 Warning

1. 这项资产是什么。
2. 它在哪里。
3. 应按什么顺序查找。
4. 需要哪些材料或设备。
5. 哪些操作有风险或不得执行。
6. 每一步的预期结果和失败停止条件。
7. 必要时联系谁。
8. 添加 Hint、Warning、自定义字段或 Attachment。

未填写“位置”和“查找顺序”的条目被标记为不完整。

### 3.5 全局说明

联系人页面强调本期不会自动通知联系人。全局说明提供“第一步”“不要做”“联系人顺序”“法律/税务提醒”等模板。

## 4. 附件

- 选择后显示文件名、类型和大小。
- 附件只在本地预览和加密。
- 不支持格式、空文件或超过上限时在加密前拒绝。
- 文件名含路径符号或控制字符时在导出阶段安全净化。
- 疑似包含高敏感秘密时显示警告；MVP 不承诺检测完整或准确。

## 5. 完整性检查

- 至少有一个资产条目。
- 所有条目有位置和查找顺序。
- 联系方式足以理解。
- 附件均可读取。
- 自定义字段已有说明。
- 高敏感内容警告已处理。
- Recovery Map 引用没有断链。
- 关键 Order 有预期结果、失败动作和 Warning。

界面以 Coverage、Findability、Sequencing、Safety、Evidence、Freshness 和 Recoverability 呈现 Recovery Confidence。只使用“需要补充／可演练／已验证”，不显示无依据的百分比。

通过后生成只读快照预览。

## 6. 设置恢复密码

1. 输入恢复密码并显示基本强度要求。
2. 再次输入确认。
3. 明确提示“CJAS 无法重置或找回此密码”。
4. 用户确认已经制定个人保存方式。

密码只在本次内存中参与密钥派生，完成或离开流程后清除引用。

## 7. 生成和保存 Kit

页面只显示“保护档案、保存加密版本、生成 Recovery Kit”等普通语言。

- 文件名示例：`我的数字资产_Vault_2026-07-31_v1.cjas`。
- 建议至少两处独立保存。
- 不要把恢复密码与 Kit 放在同一未保护位置。
- 不展示内部字段。
- 用户必须确认已下载并能找到 Kit。

## 8. 强制恢复演练

1. 清除创建流程中的明文状态和密钥引用。
2. 用户重新选择刚保存的 Kit。
3. 用户重新输入恢复密码。
4. 工具读取对应密文并执行全部校验。
5. 显示恢复后的结构化档案和附件清单。
6. 用户打开或导出至少一个附件确认。
7. 系统记录“本版本演练通过”，但不记录密码或密钥。

演练状态可以同步到账户；演练内容、Kit、Password、Data Key 和恢复结果明文不得同步。

失败时保持“待验证”，可以重试或重新生成；不得跳过并标记完成。

## 9. 更新与历史版本

- 从已完成版本复制内容生成新草稿。
- 新版本是完整快照，生成新密文、新数据密钥和新 Kit。
- 新版本不得复用旧版本的 Data Key、salt、nonce、wrapped key 或 Kit。
- 新版本必须再次完成独立恢复演练。
- 历史页显示可读名称、创建时间、说明和完成状态。
- 旧版本不会因新版本或停止续费失效。
- 隐藏历史版本只影响界面，不承诺删除永久密文。

## 10. 新设备恢复

1. 打开恢复工具。
2. 选择 `.cjas` Kit。
3. 输入恢复密码。
4. 工具自动定位、下载、校验和解密。
5. 显示恢复内容和附件导出选项。

不要求登录原账户；账户遗失、停止续费或客服不可用不得阻止恢复。网络失败不得表述为密码错误。

## 11. 平台不可用恢复

- 从预先保存的独立工具包打开恢复工具。
- Kit 内部包含公开密文定位信息。
- 自动切换备用读取端点。
- 全部失败时显示“暂时无法取得加密档案”，支持重试或导入本地密文副本。
- 不依赖 CJAS 登录、订阅状态或在线 API。

## 12. 停止续费

- 已完成版本和 Recovery Kit 不失效。
- 独立工具仍可恢复。
- 停止新建和更新服务不影响已有恢复能力。
- 停止前建议再做一次恢复演练并保存独立工具。

同样的体验适用于忘记账户、CJAS 网站关闭或公司停止运营：Kit＋Password 是恢复入口，账户不是恢复入口。

## 13. 主要错误流程

| 错误 | 用户信息 | 系统行为 |
|---|---|---|
| 密码不符合要求 | 说明最低要求 | 不生成 Kit |
| 两次密码不一致 | 请重新确认 | 清除确认框 |
| 错误恢复密码 | 密码不正确或 Kit 不匹配 | 不显示内容、不导出 |
| 错误文件类型 | 不是有效 Recovery Kit | 不继续解析 |
| Kit 版本过新 | 当前工具不支持，请更新工具 | 不降级解析 |
| Kit 被篡改 | Kit 完整性验证失败 | fail-closed |
| Kit 与密文不匹配 | 无法验证对应档案 | fail-closed |
| 密文被篡改 | 加密档案完整性验证失败 | fail-closed |
| 网络离线 | 暂时无法取得加密档案 | 允许重试/本地导入 |
| 所有端点失败 | 加密档案暂不可用 | 不误报数据丢失 |
| 附件过大 | 超过当前安全上限 | 加密前拒绝 |
| 浏览器内存不足 | 本设备无法处理此档案 | 清理状态、不产出半成品 |
| 用户取消保存 | 本版本尚未保存 | 保持草稿 |
| Kit 下载未确认 | 请先保存 Recovery Kit | 不进入完成状态 |
| 演练失败 | 本版本尚未完成验证 | 保持待验证，可重试 |
| 忘记账户 | 无需账户即可恢复 | 转到独立恢复工具 |
| Kit 与其他版本混用 | Recovery Kit 与该版本不匹配 | fail-closed |

## 14. 可访问性与隐私

- 关键状态不能只依赖颜色。
- 错误信息不得包含密码、密钥、明文或内部堆栈。
- 日志只记录阶段、错误代码和非敏感时间，不记录用户内容。
- 用户离开页面时提示未完成状态将丢失；MVP 不用浏览器持久存储自动保存明文草稿。
